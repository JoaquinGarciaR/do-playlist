<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission
        android:name="android.permission.INTERNET"
        tools:ignore="ManifestOrder" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo_background"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_secutiry_config"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Doplaylist"
        tools:targetApi="31" >
        <activity
            android:name=".SongPlaylistActivity"
            android:exported="false"
            android:label="Canciones de Playlist" />
        <activity
            android:name=".VerPlaylist"
            android:exported="false"
            android:label="Ver Playlist" />
        <activity
            android:name=".CreatePlaylistActivity"
            android:exported="false"
            android:label="Crear Playlist" />
        <activity
            android:name=".PlaylistActivity"
            android:exported="false"
            android:label="Playlists" />
        <activity
            android:name=".CancionesActivity"
            android:exported="false"
            android:label="Canciones" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="!Do-Playlist" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>